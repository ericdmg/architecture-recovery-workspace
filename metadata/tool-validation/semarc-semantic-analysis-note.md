# SemArc semantic_analysis

Status: não executado nesta etapa.

Motivo: o módulo `semantic_analysis` exige configuração de `API_KEY` e
`LLM_MODEL`. A execução pode gerar custo e precisa ser controlada
metodologicamente.

Próximo passo futuro: criar arquivo de configuração privado, não versionado, e
registrar modelo, prompt, temperatura, tokens e custo.
