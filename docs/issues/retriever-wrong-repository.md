# Retriever apontando para repositório inesperado

O submódulo `tools/retriever` não aponta para o repositório esperado.

Configurado:

```text
https://github.com/View-Based-Reverse-Engineering/Retriever
```

Esperado:

```text
https://github.com/PalladioSimulator/Palladio-ReverseEngineering-Retriever
```

Ação recomendada: corrigir o submódulo antes de adaptar a ferramenta. A URL não
foi alterada automaticamente nesta etapa.
